import express from 'express';

var router = express.Router();

import { router as router{{sample}} } from './routes/{{sample}}Route.js';

router.use('/{{sample}}', router{{sample}});

export { router };